# Local agent state, and the buffer of the event's DevEx capture hook (per developer, never committed)
.xrpl-devex/
.claude/settings.local.json
.claude/skills/
.cursor/
.codex/
.grok/

# Dependencies and build output
node_modules/
.next/
*.log

# Demo state, contains devnet seeds
.demo/

# macOS
.DS_Store
