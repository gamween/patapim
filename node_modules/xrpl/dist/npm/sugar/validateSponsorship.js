"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateSponsorship = void 0;
const bignumber_js_1 = __importDefault(require("bignumber.js"));
const errors_1 = require("../errors");
const Sponsorship_1 = require("../models/ledger/Sponsorship");
const common_1 = require("../models/transactions/common");
function validateSponsorship(client, tx, estimatedFee) {
    var _a, _b;
    return __awaiter(this, void 0, void 0, function* () {
        if (!tx.Sponsor || !tx.SponsorFlags) {
            return {
                valid: false,
                error: 'Transaction does not have Sponsor and SponsorFlags fields',
            };
        }
        if (!estimatedFee && !tx.Fee) {
            return {
                valid: false,
                error: 'No fee available to validate -- pass estimatedFee or set tx.Fee',
            };
        }
        const fee = (_a = estimatedFee !== null && estimatedFee !== void 0 ? estimatedFee : tx.Fee) !== null && _a !== void 0 ? _a : '0';
        const isCoSigned = tx.SponsorSignature != null;
        const sponsee = (_b = tx.Delegate) !== null && _b !== void 0 ? _b : tx.Account;
        try {
            const sponsorship = yield getSponsorshipEntry(client, tx.Sponsor, sponsee);
            if (!sponsorship) {
                if (!isCoSigned) {
                    return {
                        valid: false,
                        error: `No Sponsorship ledger entry found for sponsor ${tx.Sponsor} and sponsee ${sponsee}`,
                    };
                }
                return {
                    valid: true,
                    estimatedFee: fee,
                };
            }
            const isSponsoringFee = (tx.SponsorFlags & common_1.SponsorFlags.spfSponsorFee) !== 0;
            const isSponsoringReserve = (tx.SponsorFlags & common_1.SponsorFlags.spfSponsorReserve) !== 0;
            if (!isCoSigned) {
                const requiresSignForFee = (sponsorship.Flags &
                    Sponsorship_1.SponsorshipFlags.lsfSponsorshipRequireSignForFee) !==
                    0;
                const requiresSignForReserve = (sponsorship.Flags &
                    Sponsorship_1.SponsorshipFlags.lsfSponsorshipRequireSignForReserve) !==
                    0;
                if (isSponsoringFee && requiresSignForFee) {
                    return {
                        valid: false,
                        error: 'Sponsorship requires the sponsee to sign for fee sponsorship (lsfSponsorshipRequireSignForFee is set)',
                        sponsorship,
                        estimatedFee: fee,
                    };
                }
                if (isSponsoringReserve && requiresSignForReserve) {
                    return {
                        valid: false,
                        error: 'Sponsorship requires the sponsee to sign for reserve sponsorship (lsfSponsorshipRequireSignForReserve is set)',
                        sponsorship,
                        estimatedFee: fee,
                    };
                }
            }
            if (isSponsoringReserve) {
                if (sponsorship.RemainingOwnerCount == null ||
                    sponsorship.RemainingOwnerCount < 1) {
                    return {
                        valid: false,
                        error: `Sponsorship RemainingOwnerCount (${String(sponsorship.RemainingOwnerCount)}) is insufficient to cover this reserve-sponsored transaction`,
                        sponsorship,
                        estimatedFee: fee,
                    };
                }
            }
            if (!isSponsoringFee) {
                return {
                    valid: true,
                    sponsorship,
                    estimatedFee: fee,
                };
            }
            if (sponsorship.FeeAmount == null) {
                return {
                    valid: false,
                    error: 'Sponsorship FeeAmount is required when spfSponsorFee flag is set',
                    sponsorship,
                    estimatedFee: fee,
                };
            }
            if (sponsorship.FeeAmount) {
                const feeAmount = new bignumber_js_1.default(sponsorship.FeeAmount);
                const txFee = new bignumber_js_1.default(fee);
                if (feeAmount.isLessThan(txFee)) {
                    return {
                        valid: false,
                        error: `Sponsorship FeeAmount (${sponsorship.FeeAmount} drops) is insufficient for transaction fee (${fee} drops)`,
                        sponsorship,
                        estimatedFee: fee,
                    };
                }
            }
            if (sponsorship.MaxFee) {
                const maxFee = new bignumber_js_1.default(sponsorship.MaxFee);
                const txFee = new bignumber_js_1.default(fee);
                if (txFee.isGreaterThan(maxFee)) {
                    return {
                        valid: false,
                        error: `Transaction fee (${fee} drops) exceeds sponsorship MaxFee (${sponsorship.MaxFee} drops)`,
                        sponsorship,
                        estimatedFee: fee,
                    };
                }
            }
            return {
                valid: true,
                sponsorship,
                estimatedFee: fee,
            };
        }
        catch (error) {
            return {
                valid: false,
                error: error instanceof Error ? error.message : String(error),
            };
        }
    });
}
exports.validateSponsorship = validateSponsorship;
function getSponsorshipEntry(client, sponsor, sponsee) {
    return __awaiter(this, void 0, void 0, function* () {
        try {
            const request = {
                command: 'ledger_entry',
                sponsorship: {
                    sponsor,
                    sponsee,
                },
            };
            const response = yield client.request(request);
            const entry = response.result.node;
            if ((entry === null || entry === void 0 ? void 0 : entry.LedgerEntryType) === 'Sponsorship') {
                return entry;
            }
            return null;
        }
        catch (error) {
            if (error instanceof errors_1.XrplError &&
                typeof error.data === 'object' &&
                error.data !== null &&
                'error' in error.data &&
                error.data.error === 'entryNotFound') {
                return null;
            }
            throw error;
        }
    });
}
//# sourceMappingURL=validateSponsorship.js.map