"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.addPreFundedSponsor = exports.combineSponsorSigners = exports.signAsSponsor = void 0;
const fast_json_stable_stringify_1 = __importDefault(require("fast-json-stable-stringify"));
const ripple_binary_codec_1 = require("ripple-binary-codec");
const errors_1 = require("../errors");
const models_1 = require("../models");
const common_1 = require("../models/transactions/common");
const hashes_1 = require("../utils/hashes");
const utils_1 = require("./utils");
function signAsSponsor(wallet, transaction, opts = {}) {
    const tx = (0, utils_1.getDecodedTransaction)(transaction);
    if (tx.SponsorSignature) {
        throw new errors_1.ValidationError('Transaction is already signed by the sponsor.');
    }
    if ((tx.TxnSignature == null || tx.SigningPubKey == null) &&
        (tx.Signers == null || tx.Signers.length === 0)) {
        throw new errors_1.ValidationError('Transaction must be first signed by the account.');
    }
    if (tx.SponsorFlags === undefined) {
        throw new errors_1.ValidationError('Transaction must have SponsorFlags field set before sponsor can sign.');
    }
    if (tx.Sponsor === undefined) {
        throw new errors_1.ValidationError('Transaction must have Sponsor field set before sponsor can sign.');
    }
    let multisignAddress = false;
    if (typeof opts.multisign === 'string') {
        multisignAddress = opts.multisign;
    }
    else if (opts.multisign) {
        multisignAddress = wallet.classicAddress;
    }
    if (!multisignAddress &&
        !(0, common_1.areAddressesEqual)(tx.Sponsor, wallet.classicAddress)) {
        throw new errors_1.ValidationError(`Transaction Sponsor field (${tx.Sponsor}) does not match the signing wallet address (${wallet.classicAddress}).`);
    }
    if ((0, common_1.areAddressesEqual)(tx.Account, tx.Sponsor)) {
        throw new errors_1.ValidationError('signAsSponsor: Sponsor cannot be the same as the transaction Account (self-sponsorship not allowed).');
    }
    if (multisignAddress) {
        tx.SponsorSignature = {
            Signers: [
                {
                    Signer: {
                        Account: multisignAddress,
                        SigningPubKey: wallet.publicKey,
                        TxnSignature: (0, utils_1.computeSignature)(tx, wallet.privateKey, multisignAddress),
                    },
                },
            ],
        };
    }
    else {
        tx.SponsorSignature = {
            SigningPubKey: wallet.publicKey,
            TxnSignature: (0, utils_1.computeSignature)(tx, wallet.privateKey),
        };
    }
    (0, models_1.validate)(tx);
    const serialized = (0, ripple_binary_codec_1.encode)(tx);
    return {
        tx,
        tx_blob: serialized,
        hash: (0, hashes_1.hashSignedTx)(serialized),
    };
}
exports.signAsSponsor = signAsSponsor;
function combineSponsorSigners(transactions) {
    if (transactions.length === 0) {
        throw new errors_1.ValidationError('There are 0 transactions to combine.');
    }
    const decodedTransactions = transactions.map((txOrBlob) => {
        return (0, utils_1.getDecodedTransaction)(txOrBlob);
    });
    decodedTransactions.forEach((tx) => {
        var _a;
        (0, models_1.validate)(tx);
        if (((_a = tx.SponsorSignature) === null || _a === void 0 ? void 0 : _a.Signers) == null ||
            tx.SponsorSignature.Signers.length === 0) {
            throw new errors_1.ValidationError('SponsorSignature must have Signers.');
        }
        if ((tx.TxnSignature == null || tx.SigningPubKey == null) &&
            (tx.Signers == null || tx.Signers.length === 0)) {
            throw new errors_1.ValidationError('Transaction must be first signed by the account.');
        }
    });
    validateSponsorTransactionEquivalence(decodedTransactions);
    const tx = getTransactionWithAllSponsorSigners(decodedTransactions);
    return {
        tx,
        tx_blob: (0, ripple_binary_codec_1.encode)(tx),
    };
}
exports.combineSponsorSigners = combineSponsorSigners;
function validateSponsorTransactionEquivalence(transactions) {
    const exampleTransaction = (0, fast_json_stable_stringify_1.default)(Object.assign(Object.assign({}, transactions[0]), { SponsorSignature: Object.assign(Object.assign({}, transactions[0].SponsorSignature), { Signers: null }) }));
    if (transactions.slice(1).some((tx) => (0, fast_json_stable_stringify_1.default)(Object.assign(Object.assign({}, tx), { SponsorSignature: Object.assign(Object.assign({}, tx.SponsorSignature), { Signers: null }) })) !== exampleTransaction)) {
        throw new errors_1.ValidationError('Sponsor transactions are not the same.');
    }
}
function getTransactionWithAllSponsorSigners(transactions) {
    const sortedSigners = transactions
        .flatMap((tx) => { var _a, _b; return (_b = (_a = tx.SponsorSignature) === null || _a === void 0 ? void 0 : _a.Signers) !== null && _b !== void 0 ? _b : []; })
        .sort((signer1, signer2) => (0, utils_1.compareSigners)(signer1.Signer, signer2.Signer));
    const seenAccounts = new Set();
    const uniqueSigners = [];
    for (const signer of sortedSigners) {
        if (!seenAccounts.has(signer.Signer.Account)) {
            seenAccounts.add(signer.Signer.Account);
            uniqueSigners.push(signer);
        }
    }
    return Object.assign(Object.assign({}, transactions[0]), { SponsorSignature: { Signers: uniqueSigners } });
}
function addPreFundedSponsor(transaction, sponsorAddress, sponsorFlags) {
    if ((0, common_1.areAddressesEqual)(transaction.Account, sponsorAddress)) {
        throw new errors_1.ValidationError('addPreFundedSponsor: Sponsor and Account cannot be the same (self-sponsorship not allowed)');
    }
    if (!Number.isInteger(sponsorFlags)) {
        throw new errors_1.ValidationError('addPreFundedSponsor: SponsorFlags must be a valid integer');
    }
    const validFlags = common_1.SponsorFlags.spfSponsorFee | common_1.SponsorFlags.spfSponsorReserve;
    if ((sponsorFlags & ~validFlags) !== 0) {
        throw new errors_1.ValidationError('addPreFundedSponsor: SponsorFlags contains invalid flags');
    }
    if (sponsorFlags === 0) {
        throw new errors_1.ValidationError('addPreFundedSponsor: SponsorFlags must have at least one flag set');
    }
    return Object.assign(Object.assign({}, transaction), { Sponsor: sponsorAddress, SponsorFlags: sponsorFlags });
}
exports.addPreFundedSponsor = addPreFundedSponsor;
//# sourceMappingURL=sponsorSigner.js.map