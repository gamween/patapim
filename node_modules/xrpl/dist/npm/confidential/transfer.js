"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.prepareConfidentialClawback = exports.prepareConfidentialSend = void 0;
const errors_1 = require("../errors");
const ledger_1 = require("./ledger");
const loader_1 = require("./loader");
function prepareConfidentialSend(client, params) {
    var _a;
    return __awaiter(this, void 0, void 0, function* () {
        (0, ledger_1.assertConfidentialAmount)(params.amount, true);
        const [crypto, ledgerIndex] = yield Promise.all([
            (0, loader_1.loadMptCrypto)(),
            (0, ledger_1.resolveLedgerIndex)(client, params.ledgerIndex),
        ]);
        const [issuance, senderToken, destToken, sequence] = yield Promise.all([
            (0, ledger_1.fetchMPTokenIssuance)(client, params.mptIssuanceID, ledgerIndex),
            (0, ledger_1.fetchMPToken)(client, params.account, params.mptIssuanceID, ledgerIndex),
            (0, ledger_1.fetchMPToken)(client, params.destination, params.mptIssuanceID, ledgerIndex),
            (0, ledger_1.resolveSequence)(client, params.account, params.sequence),
        ]);
        const issuerKey = (0, ledger_1.requireIssuerKey)(issuance, params.mptIssuanceID);
        const { amount, senderKeypair } = params;
        const destKey = (_a = params.destinationKey) !== null && _a !== void 0 ? _a : destToken.HolderEncryptionKey;
        if (destKey == null) {
            throw new errors_1.XrplError(`Destination ${params.destination} has no registered HolderEncryptionKey`);
        }
        const { spending, version } = (0, ledger_1.resolveSpendingState)(senderToken, params.account, params.confidentialState);
        const [balance, txBlinding, rho, contextHash] = yield Promise.all([
            crypto.decryptAmount(spending, senderKeypair.privateKey, (0, ledger_1.decryptBound)(issuance, params.outstandingDelta)),
            crypto.generateBlindingFactor(),
            crypto.generateBlindingFactor(),
            crypto.getSendContextHash((0, ledger_1.accountIdHex)(params.account), params.mptIssuanceID, sequence, (0, ledger_1.accountIdHex)(params.destination), version),
        ]);
        const [amountCommitment, balanceCommitment, senderCt, destCt, issuerCt] = yield Promise.all([
            crypto.getPedersenCommitment(amount, txBlinding),
            crypto.getPedersenCommitment(balance, rho),
            crypto.encryptAmount(amount, senderKeypair.publicKey, txBlinding),
            crypto.encryptAmount(amount, destKey, txBlinding),
            crypto.encryptAmount(amount, issuerKey, txBlinding),
        ]);
        const participants = [
            { publicKey: senderKeypair.publicKey, ciphertext: senderCt },
            { publicKey: destKey, ciphertext: destCt },
            { publicKey: issuerKey, ciphertext: issuerCt },
        ];
        let auditorCt;
        if (issuance.AuditorEncryptionKey != null) {
            auditorCt = yield crypto.encryptAmount(amount, issuance.AuditorEncryptionKey, txBlinding);
            participants.push({
                publicKey: issuance.AuditorEncryptionKey,
                ciphertext: auditorCt,
            });
        }
        const tx = {
            TransactionType: 'ConfidentialMPTSend',
            Account: params.account,
            Sequence: sequence,
            MPTokenIssuanceID: params.mptIssuanceID,
            Destination: params.destination,
            SenderEncryptedAmount: senderCt,
            DestinationEncryptedAmount: destCt,
            IssuerEncryptedAmount: issuerCt,
            AmountCommitment: amountCommitment,
            BalanceCommitment: balanceCommitment,
            ZKProof: yield crypto.getConfidentialSendProof({
                privateKey: senderKeypair.privateKey,
                publicKey: senderKeypair.publicKey,
                amount,
                participants,
                txBlindingFactor: txBlinding,
                contextHash,
                amountCommitment,
                balanceParams: {
                    commitment: balanceCommitment,
                    amount: balance,
                    ciphertext: spending,
                    blindingFactor: rho,
                },
            }),
        };
        if (auditorCt != null) {
            tx.AuditorEncryptedAmount = auditorCt;
        }
        if (params.destinationTag != null) {
            tx.DestinationTag = params.destinationTag;
        }
        if (params.credentialIDs != null) {
            tx.CredentialIDs = params.credentialIDs;
        }
        return tx;
    });
}
exports.prepareConfidentialSend = prepareConfidentialSend;
function prepareConfidentialClawback(client, params) {
    var _a, _b;
    return __awaiter(this, void 0, void 0, function* () {
        if (params.amount != null) {
            (0, ledger_1.assertConfidentialAmount)(params.amount, false);
        }
        const [crypto, ledgerIndex] = yield Promise.all([
            (0, loader_1.loadMptCrypto)(),
            (0, ledger_1.resolveLedgerIndex)(client, params.ledgerIndex),
        ]);
        const [holderToken, sequence] = yield Promise.all([
            (0, ledger_1.fetchMPToken)(client, params.holder, params.mptIssuanceID, ledgerIndex),
            (0, ledger_1.resolveSequence)(client, params.account, params.sequence),
        ]);
        const { issuerKeypair } = params;
        const issuerBalance = (_a = params.issuerEncryptedBalanceOverride) !== null && _a !== void 0 ? _a : holderToken.IssuerEncryptedBalance;
        if (issuerBalance == null) {
            throw new errors_1.XrplError(`Holder ${params.holder} has no issuer-encrypted confidential balance`);
        }
        const amount = (_b = params.amount) !== null && _b !== void 0 ? _b : (yield crypto.decryptAmount(issuerBalance, issuerKeypair.privateKey, (0, ledger_1.decryptBound)(yield (0, ledger_1.fetchMPTokenIssuance)(client, params.mptIssuanceID, ledgerIndex), params.outstandingDelta)));
        const contextHash = yield crypto.getClawbackContextHash((0, ledger_1.accountIdHex)(params.account), params.mptIssuanceID, sequence, (0, ledger_1.accountIdHex)(params.holder));
        return {
            TransactionType: 'ConfidentialMPTClawback',
            Account: params.account,
            Sequence: sequence,
            MPTokenIssuanceID: params.mptIssuanceID,
            Holder: params.holder,
            MPTAmount: amount.toString(),
            ZKProof: yield crypto.getClawbackProof(issuerKeypair.privateKey, issuerKeypair.publicKey, contextHash, amount, issuerBalance),
        };
    });
}
exports.prepareConfidentialClawback = prepareConfidentialClawback;
//# sourceMappingURL=transfer.js.map